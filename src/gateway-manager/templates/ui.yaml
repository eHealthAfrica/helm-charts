apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-ui-service
data:
  ui.json: |
    {
      "name": "ui",
      "host": "http://{{ .Values.gw.aether.ui.internalService }}",
      "oidc_endpoints": [
        {
          "name": "protected",
          "url": "/",
          "strip_path": "false"
        }
      ],
      "public_endpoints": [
        {
          "name": "public",
          "url": "/-/ui/",
          "route_path": "/-/ui/",
          "strip_path": "true"
        }
      ]
    }
